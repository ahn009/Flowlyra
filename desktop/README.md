# FlowLyra Desktop Shell (Electron)

Phase 13 native desktop wrapper for macOS/Windows/Linux.

## Run

```bash
cd desktop
npm install
npm start
```

By default it opens `http://localhost:5173/inbox`.
Override with:

```bash
FLOWLYRA_DESKTOP_URL=https://your-flowlyra.example.com/inbox npm start
```
