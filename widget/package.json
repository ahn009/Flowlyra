{
  "name": "@flowlyra/widget",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "typecheck": "tsc --noEmit",
    "build:budget": "vite build --outDir .dist-budget --emptyOutDir && gzip -c .dist-budget/widget.js | wc -c | awk '{ if ($1 > 81920) { print \"Widget bundle budget exceeded: \" $1 \" bytes gz\"; exit 1 } else { print \"Widget bundle budget OK: \" $1 \" bytes gz\" } }'"
  },
  "dependencies": {
    "socket.io-client": "4.7.5"
  },
  "devDependencies": {
    "@vitejs/plugin-legacy": "5.4.1",
    "typescript": "5.5.4",
    "vite": "5.3.5"
  }
}
