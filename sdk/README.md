# FlowLyra SDKs

- `sdk/node`: Node.js client (`@flowlyra/sdk`)
- `sdk/python`: Python client (`flowlyra-sdk`)
- `sdk/php`: PHP client (`flowlyra/sdk`)

All SDKs authenticate with `X-API-Key` and target `/api/v1/platform/*` endpoints.
