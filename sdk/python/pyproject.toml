[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "flowlyra-sdk"
version = "0.1.0"
description = "Official FlowLyra API SDK for Python"
requires-python = ">=3.9"
dependencies = ["requests>=2.31.0"]
